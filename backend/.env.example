ANTHROPIC_API_KEY=sk-ant-...

# Optional: use Alpaca's Market Data API instead of yfinance. Both are
# required together — screener.py falls back to yfinance if either is
# missing. Get keys (paper trading is fine, same data access) at
# https://app.alpaca.markets/paper/dashboard/overview
APCA_API_KEY_ID=PK...
APCA_API_SECRET_KEY=...
